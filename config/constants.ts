import { NumberOfDrawsCatalog } from 'helpers/catalogs';

export const TYPEORM_CONFIG = 'database.config';
export const SERVER_PORT = 'SERVER_PORT';

export const NUMBER_OF_GROUPS = NumberOfDrawsCatalog.cpd.length;
